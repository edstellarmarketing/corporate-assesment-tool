// Credentials are now served by /api/env (Vercel serverless function).
// This file is kept as a no-op stub so local dev without the function still loads without errors.
(function (window) {
  window.env = window.env || {};
})(this);
